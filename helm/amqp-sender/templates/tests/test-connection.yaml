apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "amqp-sender.fullname" . }}-test-connection"
  labels:
{{ include "amqp-sender.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "amqp-sender.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
