apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "amqp-receiver.fullname" . }}-test-connection"
  labels:
{{ include "amqp-receiver.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "amqp-receiver.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
